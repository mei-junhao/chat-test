name = "winnicott-chat-proxy"
main = "worker.js"
compatibility_date = "2024-09-23"
# 部署后注入密钥（不要写进本文件）：
#   wrangler secret put DEEPSEEK_API_KEY
# 部署命令：
#   wrangler deploy
# 函数接收任意路径的 POST 即代理 DeepSeek（已设 stream=true），GET /health 用于探测。
